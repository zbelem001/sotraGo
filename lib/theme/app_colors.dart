import 'package:flutter/material.dart';

class AppColors {
  /// Vert SOTRACO - Couleur principale, boutons de validation, etc.
  static const Color primary = Color(0xFF008542);

  /// Jaune Vif - Actions dynamiques, bus en mouvement, alertes.
  static const Color secondary = Color(0xFFFFD700);

  /// Dark Slate / Noir bleuté - Textes principaux, fonds carte en mode sombre.
  static const Color darkSlate = Color(0xFF0F172A);

  /// Blanc Cassé / Gris très clair - Espaces de respiration, lisibilité.
  static const Color lightGray = Color(0xFFF8F9FA);

  /// Rouge Alerte - Pannes, accidents, messages d'erreur.
  static const Color alert = Color(0xFFE63946);

  /// Couleur de fond de l'application (Light)
  static const Color backgroundLight = Color(0xFFF8F9FA);

  /// Couleur de fond de l'application (Dark)
  static const Color backgroundDark = Color(0xFF0F172A);
}
